[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Ignore app directory to avoid collecting non-test classes
norecursedirs = app .git __pycache__ node_modules
addopts = 
    --verbose
    --strict-markers
    --disable-warnings
